# The manifest for the "test" environment.
# Read the full specification for the "Environment" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/environment/

# Your environment name will be used in naming your resources like VPC, cluster, etc.
name: test
type: Environment

# Import your own VPC and subnets or configure how they should be created.
# network:
#   vpc:
#     id:

# Configure the load balancers in your environment, once created.
# http:
#   public:
#   private:

# Configure observability for your environment resources.
observability:
  container_insights: false
# variables:
#   ENV: production
# secrets:
#   DJANGO_SECRET: integrations-builder/test/django-secret
#   JWT_SIGNING_KEY:
#     secretsmanager: JWT_SIGNING_KEY
#   POSTGRES_USER: 
#     secretsmanager: 'integrations-builder/test/postgres:user::'
#   POSTGRES_PASSWORD: 
#     secretsmanager: 'integrations-builder/test/postgres:password::'
#   POSTGRES_HOST: 
#     secretsmanager: 'integrations-builder/test/postgres:host::'
#   POSTGRES_PORT: 
#     secretsmanager: 'integrations-builder/test/postgres:port::'
#   POSTGRES_DB: 
#     secretsmanager: 'integrations-builder/test/postgres:db::'